import React from "react";

/*
 VirtualLab scaffold. Replace with embedded simulations (PhET), or custom HTML5 sim.
*/

export default function VirtualLab() {
    return (
        <div style={{ border: '1px dashed #888', padding: 12, borderRadius: 6 }}>
            <h3>Virtual Lab (placeholder)</h3>
            <p>Embed simulation HTML or an iframe to a virtual lab here.</p>
        </div>
    );
}
